# pm+helper 사용자 매뉴얼

- 적용 버전: V5.19.0
- 최종 업데이트: 2026-09-01
- 대상 시스템: PMPLUS20 / Microsoft SQL Server

`pm+helper`는 환자 고객 마스터, 차트번호, 처방·조제 자료의 불일치를 검사하고 제한된 범위에서 복구하는 관리자용 도구입니다. 환자 개인정보와 운영 DB를 직접 다루므로 반드시 백업과 결과 검증을 먼저 수행하십시오.

---

## 1. 반드시 지켜야 할 안전 원칙

1. 검사 결과를 확인하기 전에는 복구·삭제 버튼을 누르지 않습니다.
2. 백업 DB와 처방로그는 **판별 근거**이며 운영 DB 대신 사용하지 않습니다.
3. 같은 환자의 과거 차트번호 변경은 다른 환자 혼입과 구분합니다.
4. 복구는 환자·원차트 묶음별로 한 번씩 실행합니다.
5. PM+에서 고객을 합칠 때는 최신 정보가 있는 현재 활성 차트를 남깁니다.
6. 주민번호, 연락처, 보험정보가 포함된 CSV와 화면 캡처를 외부에 전달하지 않습니다.
7. 접속 비밀번호는 매뉴얼에 기록하지 않고 시스템 관리자에게 확인합니다.

---

## 2. 프로그램 실행과 DB 연결

### 2.1 실행

1. `pm+helper-v5.18.exe`를 마우스 오른쪽 버튼으로 클릭합니다.
2. **관리자 권한으로 실행**을 선택합니다.
3. 인증 화면이 나오면 관리자에게 부여받은 비밀번호로 로그인합니다.

관리자 권한은 SQL Server 서비스 제어, 백업 MDF 복사 및 백업 DB 연결에 필요합니다.

### 2.2 연결 설정

- 서버 주소: 일반적으로 `.\pmplus20`
- 데이터베이스: 일반적으로 `PM_MAIN`
- Windows 계정 연결: 기본 권장
- `DB 불러오기`: 연결 가능한 DB 목록 새로 고침
- `설정/위치 저장`: 현재 서버 및 DB 설정 저장

상단의 SQL 서비스 상태가 `실행중`인지 확인한 다음 작업합니다.

### 2.3 GitHub 온라인 자동 업데이트

- 프로그램 실행 시 백그라운드에서 GitHub 최신 릴리즈(`terapark3/pm-helper`)를 자동으로 확인합니다.
- 새로운 버전이 출시되었을 경우 메인 화면 상단에 `[⭐ 새 버전 가능!]` 배지가 표시되며 토스트 안내가 나타납니다.
- `[🔄 업데이트 확인]` 버튼을 클릭하면 최신 버전의 변경 내역(Release Notes)을 확인하고, **원클릭 자동 업데이트**를 통해 실행 중인 프로그램을 최신 버전으로 즉시 교체 및 재실행할 수 있습니다.

---

## 3. 주요 메뉴

- **DB 응급 복구 및 무결성 검사**: DB 상태 확인, 무결성 검사/응급 복구, DB/LOG 축소, 임시 업데이트 DB 삭제
  - `PM_DRUGUPDATE 삭제`: 약품 업데이트 후 남아있는 `PM_DRUGUPDATE` 임시 DB의 연결을 강제 종료(`SINGLE_USER`) 후 완전히 삭제
  - `PM_DURUPDATE 삭제`: DUR 업데이트 후 남아있는 `PM_DURUPDATE` 임시 DB의 연결을 강제 종료(`SINGLE_USER`) 후 완전히 삭제
  - `DB 축소` / `LOG 축소`: DB 데이터 파일 및 트랜잭션 로그 축소
- **조제고객 관련 관리**: 고객·처방·청구 관련 관리 기능
- **기초 데이터 관리**: 사용자 및 기준 데이터
- **재고관련**: 재고 데이터 관리 및 재고보정 복구
- **마약류 취급 관리**: 마약류 관련 데이터 관리
- **DB 상세 지도**: 테이블과 컬럼 구조 확인

`조제고객 관련 관리`의 주요 하위 탭:

- 차트번호 검증 및 조회
- 차트 오류 해결 도구
- 백업로그청구기반검사
- 과거 이력 관리
- 청구 누락 점검
- 처방전 내역 삭제

---

## 4. 백업 DB 연결

과거 백업의 `TBSID040_03`은 처방 당시 환자명과 차트번호를 확인하는 가장 강한 근거입니다.

### 4.1 필요한 파일

백업 폴더에 다음 파일이 있어야 합니다.

- `PM_MAIN.MDF`
- `PM_MAIN_LOG.LDF` 또는 PM_MAIN으로 시작하는 `.LDF`

### 4.2 연결 순서

1. `백업로그청구기반검사` 탭을 엽니다.
2. **백업 DB 연결**을 누릅니다.
3. MDF/LDF가 있는 백업 폴더를 선택합니다.
4. 프로그램이 SQL Server 데이터 폴더에 복사본을 만듭니다.
5. 복사본을 `PM_MAIN_BACKUP_YYYYMMDD` 이름으로 연결합니다.
6. 연결된 백업 DB를 읽기 전용으로 전환합니다.

연결 후 `백업로그청구기반검사` 상단에는 `연결된 백업 DB: DB이름 | 상태: 읽기 전용`이
항상 표시됩니다. 프로그램을 다시 실행하거나 해당 탭을 다시 열어도 SQL Server의 실제
연결 상태를 자동으로 확인합니다.

### 4.1 백업 DB 연결 해제(분리 및 임시 파일 정리)

- **백업 DB 연결 해제** 버튼을 누르면 SQL Server에서 연결된 백업 DB를 안전하게 분리(Detach)합니다.
- 분리 시 SQL 데이터 폴더에 임시로 복사되었던 파일(`PM_MAIN_BACKUP_*.mdf`, `*_log.ldf`)이 자동으로 안전하게 삭제되어 디스크 용량을 확보합니다.
- 원본 백업 폴더(예: `D:\Downloads\20260814`)의 원본 파일은 어떠한 경우에도 삭제되지 않고 온전히 유지됩니다.

원본 백업 파일은 직접 연결하거나 수정하지 않습니다. MDF/LDF 전체 크기와 추가 여유 공간이 C: 드라이브에 필요합니다.

다른 PC에서도 같은 순서로 연결할 수 있습니다. SQL DB 생성 권한이 없거나 SQL 데이터 폴더에 복사할 수 없으면 프로그램을 관리자 권한으로 다시 실행하십시오.

---

## 5. 로그·청구·백업 기반 무결성 검사

### 5.1 근거 우선순위

1. **백업 처방 원장**: 같은 `DRUG_SEQ`의 과거 환자명·차트번호
2. **처방로그**: 백업일 이후 처방의 원환자·원차트
3. **청구 자료**: 오염이 청구까지 전파됐는지 확인하는 보조 자료

청구 자료도 잘못된 현재 차트와 환자명으로 저장될 수 있으므로 청구만으로 원환자를 확정하지 않습니다.

### 5.2 검사유형 사용법

- **백업 원환자와 불일치**: 백업이 있을 때 사용하는 권장 검사
- **주민번호 암호문 불일치**: PM+ 상세 화면에서 주민번호 뒤가 별표로 남는 환자 검색
- **특정 환자/차트번호 직접 검색**: 이름이나 차트번호를 알고 있을 때 사용
- **처방로그 환자명 불일치**: 백업일 이후 자료 확인
- **로그없는 처방 포함**: 로그 누락 범위 확인
- **청구/로그 환자 불일치**: 청구 오염 여부 확인
- **다중 환자 병합 의심**: 백업이 없을 때 후보 탐색
- **전체 이상 감지**: 결과가 많을 수 있는 관리자용 종합 검사

백업이 연결되어 있으면 평소에는 `백업 원환자와 불일치`와 `특정 환자/차트번호 직접 검색`만 사용하면 됩니다.

### 5.3 검사 순서

1. 검사유형을 선택합니다.
2. 필요한 경우 검색 대상에 환자명 또는 차트번호를 입력합니다.
3. **로그/청구 기반 무결성 검사 실행**을 누릅니다.
4. 왼쪽 후보 목록에서 차트를 선택합니다.
5. 오른쪽 상세 내역에서 원장·백업·로그·청구 값을 비교합니다.

### 5.4 상세 화면의 의미

- **원장차트/원장환자명**: 현재 `TBSID040_03` 값
- **복구근거**: 백업 또는 처방로그
- **복구원차트/복구환자명**: 근거에서 확인된 과거 값
- **백업원차트/백업환자명**: 연결된 과거 DB 값
- **로그환자명/로그원차트**: `PMPLUS_JOBLOG.dbo.PM_PRES_LOG` 값
- **복구가능**: 자동 안전조건을 만족한 행

`로그환자명`, `로그주민번호`, `로그원차트`에 `(로그없음)`이 표시되면 해당 조제번호의 처방로그가 없다는 뜻입니다. 백업값이 있다면 백업으로 판정할 수 있으며, 백업과 로그가 모두 없으면 자동 복구하지 않습니다.

### 5.5 색상과 판정

- 빨간색: 백업에서 다른 원환자로 확인된 복구 후보
- 주황/경고색: 백업일 이후 로그에서 다른 원환자로 확인된 후보
- 파란색: 같은 환자의 과거 차트번호 변경 이력. 복구 대상 아님
- 노란색: 근거 부족 또는 자동판정 불가

차트번호만 다르고 환자명과 주민 식별값이 같은 행은 자동 선택되지 않습니다.

### 5.6 주민번호 암호문 불일치 검사

PM+는 정상 환자도 `JUMIN_NO`에는 마스킹된 값을 저장하고, 상세 화면에서는
`JUMIN_ENCRYPT`를 복호화하여 전체 번호를 표시합니다. 따라서 DB의 `JUMIN_NO`에
별표가 있다는 사실만으로는 오류로 판정할 수 없습니다.

검사 순서:

1. 2026-07-13 등 오류 발생 전의 백업 DB를 연결합니다.
2. 검사유형에서 **주민번호 암호문 불일치**를 선택합니다.
3. 전체 검사 또는 환자명·현재 차트번호 검색을 실행합니다.
4. `복구가능` 상태와 `충돌유형`의 상세 사유를 확인합니다.

V5.9부터는 오류 후보만 숨겨서 표시하지 않고 현재 DB의 모든 활성 고객을 다음 상태로
분류합니다.

- **복구 가능**: 이름과 주민번호 앞 7자리가 일치하고 백업 암호문이 고유한 1종이며 현재 값과 다름
- **복구 불가**: 현재 차트 또는 백업에서 암호문이 여러 종류라 자동 선택할 수 없음
- **근거 없음**: 백업에 같은 환자가 없거나 백업 암호문이 없음
- **식별 불가**: 주민번호 앞 7자리 형식이 잘못됐거나 현재 차트의 환자 식별값이 여러 개임
- **이미 정상**: 현재 암호문과 백업 암호문이 일치함

상태별 행 색상과 화면 상단 합계로 전체 분류 결과를 확인할 수 있습니다. `복구 불가`,
`근거 없음`, `식별 불가`, `이미 정상` 환자를 선택하면 복구 버튼이 잠기며 DB 변경이
차단됩니다. 실제 자동 복구는 **복구 가능** 환자에게만 허용됩니다.

V5.10 화면에서는 결과가 너무 많아지는 것을 막기 위해 기본 목록에 **복구 가능**과
**복구 불가**만 합쳐서 표시합니다. 목록 위의 버튼은 다음과 같이 동작합니다.

- **복구 대상 n명**: 복구 가능과 복구 불가 환자를 함께 표시하는 기본 화면
- **근거 없음 n명**: 클릭하면 근거 없음 환자만 별도 표시
- **식별 불가 n명**: 클릭하면 식별 불가 환자만 별도 표시
- **이미 정상 n명**: 건수만 보여주는 정보 표기이며 목록 전환 버튼이 아님

V5.17부터 네 항목은 숫자가 잘리지 않도록 버튼 폭을 실제 글자 폭보다 넉넉하게 자동
조정하며 `복구대상 107명`, `근거없음 264명`, `식별불가 4,810명`,
`이미정상 30,844명` 형식으로 표시합니다. 오른쪽의 `현재 환자 전체선택` 버튼도
체크 기호와 전체 문구가 모두 보이도록 별도로 넓게 계산합니다.

근거 없음 또는 식별 불가 목록에서도 각 환자의 상세 사유와 처방은 확인할 수 있지만
복구 버튼은 잠겨 있습니다. 다시 기본 목록으로 돌아가려면 **복구 대상 n명**을 누릅니다.

암호문 복구 순서:

1. 먼저 **백업 원환자와 불일치** 검사를 실행하여 한 차트에 섞인 다른 환자의 처방을 모두 원환자·원차트로 분리합니다.
2. **백업 원환자와 불일치** 검사를 다시 실행하여 해당 차트에 불일치가 남지 않았는지 확인합니다.
3. 왼쪽 검사 결과에서 `복구 가능` 환자 한 명을 클릭합니다. 왼쪽 목록은 환자 선택·조회용이며 체크박스를 사용하지 않습니다.
4. 오른쪽 상세 처방이 선택한 환자의 자료인지 확인합니다.
5. **현재 환자 전체선택**을 눌러 오른쪽 처방 전체를 체크합니다. 일부 처방만 선택된 상태에서는 복구 버튼이 활성화되지 않습니다.
6. **현재 환자 암호문 복구**를 누릅니다.
7. 환자 마스터·처방·청구 등 테이블별 변경 예정 건수를 확인합니다.
8. PMPLUS20에서 해당 환자 화면을 닫고 다른 사용자가 수정 중이 아닌지 확인합니다.
9. 두 번의 확인 창에서 내용이 맞을 때만 복구를 실행합니다.
10. 완료 후 검사 목록에서 해당 환자가 `이미 정상`으로 바뀌었는지, PMPLUS20 상세 화면에서 주민번호와 과거 조제가 정상 표시되는지 확인합니다.

V5.15부터 선택 환자의 현재 차트에 백업으로 확인되는 다른 원환자 처방이 한 건이라도
남아 있으면 `복구 불가`로 표시합니다. `선행복구필요건수`와 충돌 사유에서 건수를
확인할 수 있습니다. 화면 표시를 우회하더라도 실제 저장 직전 트랜잭션에서 같은 조건을
다시 검사하며, 남아 있으면 암호문 복구를 전부 취소합니다.

V5.16부터 암호문 복구 화면의 오른쪽 열은 차트 복구 화면과 구분하여 표시합니다.

- `현재차트(변경안함)`: 암호문 복구로 차트번호가 바뀌지 않음
- `차트근거(참고)`, `과거차트(참고)`: 차트 소유권을 판단할 때만 쓰는 참고정보
- `백업/처방로그(동일환자 과거차트 참고)`: 같은 환자가 과거에 사용한 차트이며 이동 대상이 아님
- `차트근거 없음(암호문 복구와 무관)`: 원차트를 확인할 수 없지만 환자 단위 암호문 복구 가능 여부와는 별개
- `현재 환자 암호문만 복구`: 차트번호·환자명·처방내용을 변경하지 않고 검증된 암호문만 변경

복구는 백업의 `JUMIN_ENCRYPT`만 사용하며 주민번호 표시값이나 처방 내용은 바꾸지 않습니다.
적용 대상은 존재하는 테이블에 한해 환자 마스터 `TBSIT000_01`, 처방·조제
`TBSID040_03`, 청구 `TBSIB_H024_1`, 고객 메모 `TBSIT000_02`, 차트 매핑
`TEMP_MAPPING_CHRTNO`, `TEMP_MAPPING_CHRTNO_SUB`입니다.

미리보기 후 실제 실행 시 대상 건수를 다시 잠금 상태로 계산합니다. 건수가 달라지거나
한 테이블이라도 저장·검증에 실패하면 전체 트랜잭션을 취소하므로 일부 테이블만 바뀐
상태가 남지 않습니다. 변경 전 암호문은
`PM_HELPER_JUMIN_ENCRYPT_RESTORE_AUDIT`에 세션 ID와 함께 저장됩니다. 감사 테이블에는
평문 주민번호가 아니라 기존·복구 암호문만 보관됩니다.

오른쪽 처방 선택은 현재 환자의 복구 범위를 확인하기 위한 표시입니다. 암호문은 처방 한
건만 고치는 값이 아니므로 실제 복구에서는 환자 마스터·처방·청구·고객 메모·차트 매핑의
동일 환자 관련 행을 모두 같은 트랜잭션과 감사 세션 ID로 처리합니다. 환자 식별값 또는
변경 건수가 미리보기와 달라지면 전체를 롤백하며 일부 테이블만 복구된 상태를 남기지 않습니다.

`백업 원환자와 불일치` 복구는 잘못 합쳐진 처방의 차트번호·환자명·소유자를 먼저
바로잡는 작업입니다. 이 작업이 주민번호 암호문까지 항상 수정하는 것은 아닙니다.
따라서 차트 복구가 끝난 뒤 주민번호 암호문 검사를 다시 실행해야 합니다. 암호문이
백업과 다르면 계속 `복구 가능`으로 표시되고, 차트 식별관계가 정리되면서 이전의
`복구 불가`가 `복구 가능`으로 바뀔 수도 있습니다. 암호문이 이미 맞으면 `이미 정상`으로 표시됩니다.

---

## 6. 환자·원차트별 선택과 복구

### 6.1 환자별 선택

상단 `복구환자` 목록에는 다음 형식으로 복구 가능한 묶음만 표시됩니다.

`환자명 / 원차트번호 (건수)`

예: `송우연 / 0000299486 (66건)`

1. `복구환자` 목록에서 한 묶음을 선택합니다.
2. **환자별 선택**을 누릅니다.
3. 해당 환자와 해당 원차트의 처방만 체크됐는지 확인합니다.
4. 잘못 선택했으면 **해제**를 누릅니다.
5. 원차트번호와 선택 건수를 다시 확인합니다.

같은 환자라도 과거 원차트가 두 개면 두 묶음으로 나옵니다. 서로 다른 원차트는 한 번에 처리하지 않습니다.

### 6.2 V5.13 복구의 실제 변경 범위

V5.13의 **선택 처방을 원차트로 분리/복구**는 다음 작업을 수행합니다.

- `TBSID040_03`의 `CHRTNO`, `PAT_NM`, `PAT_JUMIN_NO`, `JUMIN_ENCRYPT` 변경
- 대상 원차트 고객이 현재 DB에 없으면 먼저 현재 고객 원장에서 동일 이름과 주민번호 앞 7자리의 고유 식별값을 확인
- 현재 고객 원장에도 원본 환자가 없으면 읽기 전용 백업 DB의 정확한 원차트에서 고객 이력 전체를 복원
- 변경 전 값을 `PM_HELPER_CHART_RESTORE_BACKUP`에 저장
- 백업 고객 원장 복원 기록을 `PM_HELPER_CUSTOMER_RESTORE_AUDIT`에 저장

백업 고객 원장 복원은 다음 조건을 모두 만족할 때만 허용됩니다.

- 백업 원차트번호가 선택 처방의 복구 원차트번호와 정확히 같음
- 백업 원차트의 환자명이 선택 환자명 한 종류로 확인됨
- 주민번호 앞 7자리가 처방 복구 근거와 한 종류로 일치함
- `JUMIN_ENCRYPT`가 비어 있지 않고 한 종류로 확정됨
- 활성 고객 행(`CUSACT=1`)이 정확히 1개임

조건 하나라도 맞지 않거나 백업 DB가 읽기 전용이 아니면 고객 생성과 처방 이동을 모두
중단합니다. 고객 이력 복원과 처방 이동은 같은 트랜잭션으로 처리되므로 중간 오류가
발생하면 전부 롤백됩니다.

다음 자료는 직접 변경하지 않습니다.

- `TBSID040_04`, `TBSID040_05`: `DRUG_SEQ` 연결을 그대로 유지
- `TBSIB_H024_1`: 청구 자료
- `TBSIR000_01`: 수납·관련 이력
- `PM_PRES_LOG`: 처방로그

따라서 이 기능을 모든 관련 테이블을 한 번에 합치는 기능으로 오해하면 안 됩니다.

### 6.3 원차트 복구와 현재 차트 통합의 차이

V5.3은 백업·로그에서 확인한 **과거 원차트번호**로 처방을 되돌립니다. 현재 PM+ 고객 화면이 최신 활성 차트번호만 기준으로 과거조제내역을 조회하면, 원차트로 복구된 처방이 현재 차트 화면에 함께 보이지 않을 수 있습니다.

이 경우 데이터가 사라진 것이 아닙니다. `조제현황`에서 과거 원차트 또는 환자명으로 조회하면 복구된 처방을 확인할 수 있습니다.

모든 과거내역을 최신 활성 차트에서 보이게 하려는 목적이라면 원차트 복구 후 PM+의 고객합치기를 별도로 검토해야 합니다.

고객합치기 방향:

- **남길 고객**: 최신 보험·연락처·부가정보가 있는 현재 활성 차트
- **합쳐질 고객**: 복구 도구가 생성했거나 과거에 사용한 원차트

거꾸로 합치면 현재 활성 차트가 없어지거나 최신 고객정보가 오래된 값으로 바뀔 수 있습니다. 합치기 전에는 두 행의 차트번호를 반드시 확인하고 DB 백업을 확보하십시오.

### 6.4 과거 이력 관리 하단 버튼

V5.18부터 과거 이력 관리 화면의 `수정사항 저장`, `선택 이력 삭제` 버튼은 하단
패널 높이와 글자 폭에 맞춰 자동 배치됩니다. 중앙 이력 표가 하단 버튼 영역을 덮지
않도록 하단 패널을 항상 앞으로 유지합니다.

---

## 7. 송우연 사례 확인 기준

2026-07-13 백업과 처방로그를 기준으로 확인된 값:

- `0000299486 / 송우연`: 백업으로 확인된 66건
- `0000336491 / 송우연`: 백업일 이후 로그로 확인된 1건
- `0000463116 / 송우연`: 2026-08-14 현재 처방 1건

김영금 차트에 혼입된 송우연 처방은 67건이며, 현재 정상 처방 1건까지 합하면 송우연 처방은 총 68건입니다.

66건을 `0000299486`으로 원차트 복구한 직후에는:

- `조제현황`: 송우연 검색 시 66건 확인 가능
- `조제(수정) > 과거조제내역`: 최신 활성 차트 `0000463116`의 1건만 보일 수 있음

이는 조회 기준 차트가 달라서 생기는 현상이며 데이터 유실이 아닙니다.

---

## 8. 차트 오류 해결 도구

### 8.1 전체 검사

1. `차트 오류 해결 도구` 탭을 엽니다.
2. **전체 테이블 검사 실행(스캔)**을 누릅니다.
3. 개명·중복 차트와 처방전 혼선 후보를 확인합니다.

### 8.2 고객 마스터 복구

차트 주인의 이름이나 주민번호가 잘못된 경우 상세 조회 후 올바른 고객 정보를 선택하여 복구합니다. 이름이 같다는 이유만으로 선택하지 말고 주민번호 앞 7자리와 암호화 식별값을 함께 확인하십시오.

### 8.3 처방 분리 이동

처방을 다른 차트로 이동하기 전 출발 차트, 대상 차트, 환자명, 주민번호와 처방 건수를 확인합니다. 신규 차트를 만들기보다 현재 존재하는 검증된 활성 차트를 우선 검토하십시오.

---

## 9. CSV 내보내기

검사 결과는 **결과 CSV 저장**으로 내보낼 수 있습니다.

- UTF-8 BOM 형식으로 저장되어 Excel에서 한글이 정상 표시됩니다.
- 저장한 CSV에는 환자 개인정보가 포함될 수 있습니다.
- 보고가 끝난 파일은 병원·약국의 개인정보 보관 규정에 따라 관리합니다.

---

## 10. 기타 관리 기능

### 사용자 ID 관리

`TBSIM000_09`의 사용자 정보를 관리합니다. 비밀번호는 복호화해서 확인할 수 없으며 필요한 경우 새 비밀번호로 재설정합니다.

### 카드결제내역 관리

`TBSIR000_01` 자료를 수납일자와 차트번호로 조회합니다. 금액·승인번호 수정 또는 삭제 전 원자료를 확인하십시오.

### 라벨출력정보 관리

`TBSIM040_43`의 약품 라벨 정보를 조회하고 수정합니다.

### 처방전 내역 삭제

`TBSID040_03`, `TBSID040_04`, `TBSID040_05` 관련 일반 처방을 삭제하는 기능입니다. 재고보정(`099999` 또는 `PRES_GUBUN='E'`) 데이터는 처방 삭제 메뉴에서 임의로 삭제되지 않도록 원천 보호됩니다.

### 재고보정 복구 (신규 기능)

`재고관련` > `재고보정 복구` 탭에서 과거 백업 DB와 현재 운영 DB 간의 재고보정 내역을 전수 비교하고 누락된 상세 약품을 1-클릭으로 안전하게 복원합니다.

1. **백업 DB 연결**: `[백업 DB 연결]`을 눌러 백업 MDF/LDF 폴더를 선택합니다.
2. **대조 검사 실행**: `[재고보정 대조 검사 실행]`을 누르면 운영 DB와 백업 DB 간의 모든 보정 작업(`PRES_GUBUN='E'`, `099999`)을 비교합니다.
3. **상세 내역 확인**: 좌측 목록에서 보정일자를 클릭하면 우측에 백업 약품 목록과 운영 누락 상태가 상세히 대조 표시됩니다.
4. **복구 실행**:
   - `[선택 일자 복구]`: 체크된 특정 일자만 복구
   - `[누락 전체 일괄 복구]`: 누락된 모든 보정일자의 `TBSID040_03`(헤더), `TBSID040_04`(약품상세), `TBSID040_05`(조제상세), `TBSIM040_08`(보정기준)을 트랜잭션으로 일괄 복원
5. **백업 DB 연결 해제**: 복구 완료 후 `[백업 DB 연결 해제]`를 눌러 임시 마운트된 백업 DB를 안전하게 분리하고 디스크 용량을 환원합니다.

---

## 11. 작업 후 검증 체크리스트

- [ ] 예상한 조제번호만 변경됐는가
- [ ] 출발 차트와 대상 차트의 처방 건수 합계가 보존됐는가
- [ ] 환자명과 주민번호 앞 7자리가 일치하는가
- [ ] `TBSID040_04`, `TBSID040_05` 상세 약품이 정상적으로 열리는가
- [ ] 조제현황에서 복구된 처방을 조회할 수 있는가
- [ ] 현재 활성 차트의 과거조제내역 표시 방식이 의도와 맞는가
- [ ] 고객 마스터가 중복 활성 상태가 되지 않았는가
- [ ] 청구·수납 자료가 별도로 남아 있는지 확인했는가
- [ ] `PM_HELPER_CHART_RESTORE_BACKUP`에 변경 전 값이 저장됐는가
- [ ] 백업 고객 원장을 복원했다면 `PM_HELPER_CUSTOMER_RESTORE_AUDIT`에 같은 세션 ID와 복원 행 수가 저장됐는가
- [ ] 암호문 복구 후 해당 환자가 재검사에서 `이미 정상`으로 바뀌었는가
- [ ] `PM_HELPER_JUMIN_ENCRYPT_RESTORE_AUDIT`에 완료 메시지의 감사 세션 ID가 저장됐는가

건수나 환자 식별값이 하나라도 맞지 않으면 추가 작업을 중단하고 DB 관리자에게 확인합니다.

---

## 12. 자주 묻는 질문

### 로그환자명과 로그원차트가 비어 있거나 `(로그없음)`으로 나옵니다.

해당 조제번호의 처방로그가 없다는 뜻입니다. 백업값이 있으면 백업을 근거로 판정하며, 백업도 없으면 자동 복구하지 않습니다.

### 복구했는데 PM+ 과거조제내역에 보이지 않습니다.

복구된 처방은 과거 원차트에 있고 PM+ 화면은 최신 활성 차트를 조회할 수 있습니다. 먼저 조제현황에서 환자명 또는 원차트로 처방이 존재하는지 확인하십시오.

### 같은 환자가 고객검색에 두 줄로 나옵니다.

과거 원차트 고객 마스터와 현재 활성 차트가 함께 존재하는 상태입니다. 최신 활성 차트를 남기는 방향으로 고객합치기를 검토하되, 실행 전 차트번호와 최신 고객정보를 반드시 확인하십시오.

### 고객을 거꾸로 합치면 어떻게 됩니까?

과거 원차트가 남고 최신 활성 차트가 없어지거나 비활성화될 수 있습니다. 최신 보험·연락처·부가정보가 유실될 위험이 있으므로 권장하지 않습니다.

### 검사 결과가 너무 많습니다.

백업이 연결되어 있다면 `백업 원환자와 불일치`를 사용하십시오. 동일 환자의 단순 차트번호 변경은 파란색으로 표시되고 복구 대상에서 제외됩니다.

### SQL 서비스 시작·중지 시 권한 창이 나옵니다.

Windows 서비스 제어는 관리자 권한이 필요합니다. UAC 창에서 작업 내용을 확인한 뒤 승인하십시오.

### 프로그램이 멈춘 것처럼 보입니다.

대용량 전체 검사는 수십 초 이상 걸릴 수 있습니다. 같은 검사를 반복 실행하지 말고 완료 메시지를 기다립니다.

---

## 13. 장애 발생 시 기록할 정보

- 실행 파일 버전
- 작업 일시
- 서버와 DB 이름
- 출발 차트와 대상 차트
- 환자명과 마스킹된 주민번호 앞 7자리
- 선택한 조제번호와 건수
- 오류 메시지 전체
- `PM_HELPER_CHART_RESTORE_BACKUP`의 세션 ID
- 암호문 복구인 경우 `PM_HELPER_JUMIN_ENCRYPT_RESTORE_AUDIT`의 감사 세션 ID

환자 개인정보가 노출된 화면 캡처는 외부 메신저나 공개 저장소에 업로드하지 않습니다.
